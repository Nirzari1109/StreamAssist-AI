# StreamAssist UI

Streamlit demo UI for the StreamAssist multi-agent support assistant.

## Run Backend First

From the project root:

```bash
.venv\Scripts\activate
uvicorn app.main:app --reload